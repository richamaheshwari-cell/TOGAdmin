# Backend API base URL (required for API calls and Next.js rewrites).
# Local dev: backend usually runs on port 3000; admin panel can run on 3001.
# Production: set to your deployed backend URL (e.g. https://api.yoursite.com).
NEXT_PUBLIC_API_URL=https://api.theoceangame.com
